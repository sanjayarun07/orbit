.env
.env.*
!.env.example
.claude/
.vscode/
.venv/
node_modules/
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.DS_Store

# Investigation snapshots, API test captures and runtime state (role_memory.json,
# dspy cache) stay local; only the optimizer examples/README are versioned.
data/*
!data/optimized_prompts/

# scripts/optimize_signature.py's output -- a local artifact to inspect
# manually, not something to commit (examples/README alongside it are kept).
data/optimized_prompts/trade_simulation_compiled.json

# routing eval working files (generated by collect.py / harness.py --mode disagree)
scripts/routing_eval/candidates.json
scripts/routing_eval/to_label.json

# synthesis eval working files (generated by scripts/synthesis_eval)
scripts/synthesis_eval/bundles.json
scripts/synthesis_eval/results.json
scripts/synthesis_eval/runs/
